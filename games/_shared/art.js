export const THEMES = {
  meadow: { sky: ["#7dd3fc", "#bbf7d0"], hill: "#4ade80", hill2: "#86efac", field: "#bbf7d0", floor: "grass", dress: "garden", sun: "#fde047" },
  candy: { sky: ["#fce7f3", "#fde68a"], hill: "#fb7185", hill2: "#f9a8d4", field: "#fbcfe8", floor: "polka", dress: "sweets", sun: "#fb7185" },
  ice: { sky: ["#e0f2fe", "#7dd3fc"], hill: "#bae6fd", hill2: "#e0f2fe", field: "#e0f2fe", floor: "ice", dress: "pines", sun: "#fff" },
  sunset: { sky: ["#38bdf8", "#f9a8d4"], hill: "#fda4af", hill2: "#fde68a", field: "#fecdd3", floor: "dusk", dress: "birds", sun: "#fb923c" },
  sea: { sky: ["#67e8f9", "#0369a1"], hill: "#0ea5e9", hill2: "#22d3ee", field: "#0ea5e9", floor: "water", dress: "reef", sun: "#fde047" },
  jungle: { sky: ["#fde68a", "#86efac"], hill: "#22c55e", hill2: "#4ade80", field: "#86efac", floor: "grass", dress: "vines", sun: "#facc15" },
  night: { sky: ["#1e1b4b", "#312e81"], hill: "#312e81", hill2: "#1e1b4b", field: "#1e1b4b", floor: "stars", dress: "moon", sun: "#f8fafc" },
  paint: { sky: ["#fda4af", "#7dd3fc"], hill: "#fb7185", hill2: "#38bdf8", field: "#f5d0fe", floor: "splats", dress: "drips", sun: "#facc15" },
  party: { sky: ["#fce7f3", "#fde68a"], hill: "#f472b6", hill2: "#fbbf24", field: "#fbcfe8", floor: "confetti", dress: "balloons", sun: "#f472b6" },
  track: { sky: ["#fdba74", "#facc15"], hill: "#f97316", hill2: "#fde68a", field: "#fed7aa", floor: "asphalt", dress: "crowd", sun: "#facc15" },
  snow: { sky: ["#e0f2fe", "#93c5fd"], hill: "#fff", hill2: "#dbeafe", field: "#f8fafc", floor: "snow", dress: "pines", sun: "#fff" },
  grass: { sky: ["#86efac", "#4ade80"], hill: "#16a34a", hill2: "#86efac", field: "#4ade80", floor: "pitch", dress: "bleachers", sun: "#fde047" },
  sand: { sky: ["#fde68a", "#fb923c"], hill: "#fbbf24", hill2: "#fdba74", field: "#fde68a", floor: "sand", dress: "beach", sun: "#fb923c" },
  pong: { sky: ["#fb7185", "#fbbf24"], hill: "#fb7185", hill2: "#fde68a", field: "#1e1b4b", floor: "court", dress: "lights", sun: "#fde047" },
  "ice rink": { sky: ["#38bdf8", "#e0f2fe"], hill: "#7dd3fc", hill2: "#e0f2fe", field: "#bae6fd", floor: "ice", dress: "rink", sun: "#fff" },
  gold: { sky: ["#fde047", "#fb7185"], hill: "#f59e0b", hill2: "#fbbf24", field: "#fde68a", floor: "sparkle", dress: "stars", sun: "#facc15" },
  potato: { sky: ["#fdba74", "#fb7185"], hill: "#ea580c", hill2: "#fdba74", field: "#ffedd5", floor: "tile", dress: "kitchen", sun: "#fb923c" },
  crate: { sky: ["#fef3c7", "#86efac"], hill: "#ca8a04", hill2: "#fde68a", field: "#fef3c7", floor: "wood", dress: "boxes", sun: "#fbbf24" },
  mirror: { sky: ["#ede9fe", "#f9a8d4"], hill: "#c4b5fd", hill2: "#ddd6fe", field: "#ede9fe", floor: "glass", dress: "mirrors", sun: "#c4b5fd" },
  bright: { sky: ["#fff7ed", "#bae6fd"], hill: "#fdba74", hill2: "#fde68a", field: "#ffedd5", floor: "playground", dress: "toys", sun: "#fbbf24" },
  cards: { sky: ["#f3e8ff", "#fde68a"], hill: "#a78bfa", hill2: "#ddd6fe", field: "#5b21b6", floor: "felt", dress: "table", sun: "#a78bfa" },
  tower: { sky: ["#fff7ed", "#fdba74"], hill: "#fb923c", hill2: "#fde68a", field: "#ffedd5", floor: "brick", dress: "skyline", sun: "#fb923c" },
  pillow: { sky: ["#f472b6", "#818cf8"], hill: "#ec4899", hill2: "#c084fc", field: "#fbcfe8", floor: "soft", dress: "cloudbed", sun: "#fde047" },
  frost: { sky: ["#e0f2fe", "#38bdf8"], hill: "#fff", hill2: "#bae6fd", field: "#e0f2fe", floor: "snow", dress: "pines", sun: "#fff" },
  sumo: { sky: ["#fde68a", "#f97316"], hill: "#fb923c", hill2: "#fde047", field: "#ffedd5", floor: "dojo", dress: "lanterns", sun: "#facc15" },
  coins: { sky: ["#facc15", "#fb923c"], hill: "#f59e0b", hill2: "#fde68a", field: "#fde68a", floor: "vault", dress: "coins", sun: "#facc15" },
  orchard: { sky: ["#86efac", "#fb7185"], hill: "#22c55e", hill2: "#bbf7d0", field: "#bbf7d0", floor: "grass", dress: "apples", sun: "#fde047" },
  soap: { sky: ["#67e8f9", "#a78bfa"], hill: "#22d3ee", hill2: "#c4b5fd", field: "#a5f3fc", floor: "water", dress: "bubbles", sun: "#67e8f9" },
  pets: { sky: ["#86efac", "#f9a8d4"], hill: "#4ade80", hill2: "#fbcfe8", field: "#bbf7d0", floor: "grass", dress: "yard", sun: "#fde047" },
  noodle: { sky: ["#dcfce7", "#86efac"], hill: "#14532d", hill2: "#22c55e", field: "#14532d", floor: "broth", dress: "steam", sun: "#fde047" },
  glow: { sky: ["#020617", "#0f172a"], hill: "#022c22", hill2: "#164e63", field: "#020617", floor: "stars", dress: "neon", sun: "#22d3ee" },
  blocks: { sky: ["#1e1b4b", "#312e81"], hill: "#1e1b4b", hill2: "#4c1d95", field: "#1e1b4b", floor: "stars", dress: "moon", sun: "#a78bfa" },
  connect: { sky: ["#fff7ed", "#dbeafe"], hill: "#1d4ed8", hill2: "#93c5fd", field: "#1d4ed8", floor: "felt", dress: "table", sun: "#60a5fa" },
  mole: { sky: ["#86efac", "#4ade80"], hill: "#365314", hill2: "#65a30d", field: "#bbf7d0", floor: "dirt", dress: "garden", sun: "#fde047" },
  spark: { sky: ["#312e81", "#1e1b4b"], hill: "#4c1d95", hill2: "#312e81", field: "#1e1b4b", floor: "stars", dress: "neon", sun: "#fde047" }
};

function oval(g, x, y, rx, ry, color) {
  g.fillStyle = color;
  g.beginPath();
  g.ellipse(x, y, rx, ry, 0, 0, Math.PI * 2);
  g.fill();
}

function tree(g, x, y, s, leaf, trunk) {
  g.fillStyle = trunk || "#92400e";
  g.fillRect(x - s * 0.12, y - s * 0.2, s * 0.24, s * 0.55);
  oval(g, x, y - s * 0.35, s * 0.42, s * 0.38, leaf || "#16a34a");
  oval(g, x - s * 0.22, y - s * 0.18, s * 0.28, s * 0.24, leaf || "#22c55e");
  oval(g, x + s * 0.2, y - s * 0.16, s * 0.26, s * 0.22, leaf || "#4ade80");
}

function pine(g, x, y, s) {
  g.fillStyle = "#92400e";
  g.fillRect(x - s * 0.08, y, s * 0.16, s * 0.28);
  g.fillStyle = "#047857";
  for (let i = 0; i < 3; i++) {
    g.beginPath();
    g.moveTo(x, y - s * 0.72 + i * s * 0.18);
    g.lineTo(x - s * (0.38 - i * 0.06), y - s * 0.28 + i * s * 0.18);
    g.lineTo(x + s * (0.38 - i * 0.06), y - s * 0.28 + i * s * 0.18);
    g.fill();
  }
}

function cloud(g, x, y, s, color) {
  oval(g, x, y, s, s * 0.58, color || "rgba(255,255,255,.82)");
  oval(g, x + s * 0.55, y + s * 0.08, s * 0.72, s * 0.48, color || "rgba(255,255,255,.75)");
  oval(g, x - s * 0.45, y + s * 0.1, s * 0.55, s * 0.4, color || "rgba(255,255,255,.7)");
}

export function paintWorld(g, ctx, name) {
  const th = THEMES[name] || THEMES.meadow;
  const t = ctx.t || 0;
  const sky = g.createLinearGradient(0, 0, 0, ctx.h);
  sky.addColorStop(0, th.sky[0]);
  sky.addColorStop(1, th.sky[1]);
  g.fillStyle = sky;
  g.fillRect(0, 0, ctx.w, ctx.h);

  const night = th.floor === "stars";
  if (night) {
    g.fillStyle = "rgba(255,255,255,.45)";
    for (let i = 0; i < 42; i++) {
      const x = ((i * 97 + t * 10) % (ctx.w + 20));
      const y = (i * 47 + 16) % (ctx.h * 0.62);
      g.globalAlpha = 0.35 + (i % 5) * 0.12;
      g.beginPath();
      g.arc(x, y, i % 7 === 0 ? 2.4 : 1.2, 0, Math.PI * 2);
      g.fill();
    }
    g.globalAlpha = 1;
    oval(g, ctx.w * 0.82, 70, 28, 28, th.sun);
    oval(g, ctx.w * 0.8, 64, 22, 22, th.sky[0]);
  } else {
    oval(g, ctx.w * 0.82, 64, 34, 34, th.sun);
    g.fillStyle = "rgba(255,255,255,.28)";
    g.beginPath();
    g.arc(ctx.w * 0.82 - 8, 56, 12, 0, Math.PI * 2);
    g.fill();
    for (let i = 0; i < 5; i++) {
      const x = ((t * (14 + i * 5) + i * 170) % (ctx.w + 120)) - 50;
      cloud(g, x, 44 + (i % 3) * 26, 22 + (i % 2) * 6);
    }
  }

  g.fillStyle = th.hill2;
  g.beginPath();
  g.moveTo(0, ctx.h);
  for (let x = 0; x <= ctx.w; x += 16) g.lineTo(x, ctx.h * 0.74 + Math.sin(x * 0.012 + name.length) * 20);
  g.lineTo(ctx.w, ctx.h);
  g.closePath();
  g.fill();
  g.fillStyle = th.hill;
  g.beginPath();
  g.moveTo(0, ctx.h);
  for (let x = 0; x <= ctx.w; x += 14) g.lineTo(x, ctx.h * 0.84 + Math.sin(x * 0.016 + 2) * 12);
  g.lineTo(ctx.w, ctx.h);
  g.closePath();
  g.fill();

  paintDress(g, ctx, th, "far");
  paintField(g, ctx, th);
  paintDress(g, ctx, th, "near");
}

function paintField(g, ctx, th) {
  const f = ctx.field;
  g.save();
  g.beginPath();
  g.roundRect(f.x, f.y, f.w, f.h, 28);
  g.clip();
  g.fillStyle = th.field;
  g.fillRect(f.x, f.y, f.w, f.h);
  const floor = th.floor;
  if (floor === "grass" || floor === "pitch") {
    for (let i = 0; i < 9; i++) {
      g.fillStyle = i % 2 ? "rgba(22,163,74,.18)" : "rgba(255,255,255,.12)";
      g.fillRect(f.x + (f.w / 9) * i, f.y, f.w / 9, f.h);
    }
    if (floor === "pitch") {
      g.strokeStyle = "rgba(255,255,255,.55)";
      g.lineWidth = 3;
      g.strokeRect(f.x + 18, f.y + 18, f.w - 36, f.h - 36);
      g.beginPath();
      g.moveTo(f.x + f.w / 2, f.y + 18);
      g.lineTo(f.x + f.w / 2, f.y + f.h - 18);
      g.stroke();
      g.beginPath();
      g.arc(f.x + f.w / 2, f.y + f.h / 2, 36, 0, Math.PI * 2);
      g.stroke();
    }
    g.fillStyle = "rgba(22,163,74,.35)";
    for (let i = 0; i < 40; i++) {
      const x = f.x + 10 + ((i * 73) % (f.w - 20));
      const y = f.y + 30 + ((i * 47) % (f.h - 40));
      g.fillRect(x, y, 2, 7);
    }
  } else if (floor === "ice" || floor === "snow") {
    g.fillStyle = "rgba(255,255,255,.35)";
    g.beginPath();
    g.moveTo(f.x, f.y + f.h * 0.2);
    g.lineTo(f.x + f.w * 0.4, f.y);
    g.lineTo(f.x + f.w * 0.55, f.y);
    g.lineTo(f.x, f.y + f.h * 0.45);
    g.fill();
    g.fillStyle = "rgba(125,211,252,.25)";
    for (let i = 0; i < 18; i++) {
      g.beginPath();
      g.arc(f.x + ((i * 89) % f.w), f.y + ((i * 53) % f.h), 10, 0, Math.PI * 2);
      g.fill();
    }
  } else if (floor === "water") {
    g.fillStyle = "rgba(255,255,255,.16)";
    for (let i = 0; i < 8; i++) {
      g.beginPath();
      const yy = f.y + 20 + i * (f.h / 8) + Math.sin(ctx.t * 2 + i) * 4;
      g.moveTo(f.x, yy);
      for (let x = 0; x <= f.w; x += 16) g.lineTo(f.x + x, yy + Math.sin(x * 0.04 + ctx.t * 2 + i) * 5);
      g.strokeStyle = "rgba(255,255,255,.28)";
      g.lineWidth = 2;
      g.stroke();
    }
  } else if (floor === "polka" || floor === "confetti") {
    const cols = ["#fb7185", "#38bdf8", "#fbbf24", "#a78bfa", "#4ade80"];
    for (let i = 0; i < 36; i++) {
      g.fillStyle = cols[i % cols.length];
      g.globalAlpha = 0.35;
      g.beginPath();
      g.arc(f.x + ((i * 67) % f.w), f.y + ((i * 41) % f.h), floor === "confetti" ? 5 : 8, 0, Math.PI * 2);
      g.fill();
    }
    g.globalAlpha = 1;
  } else if (floor === "wood" || floor === "dojo") {
    for (let i = 0; i < 12; i++) {
      g.fillStyle = i % 2 ? "rgba(180,83,9,.18)" : "rgba(253,230,138,.2)";
      g.fillRect(f.x, f.y + i * (f.h / 12), f.w, f.h / 12);
    }
  } else if (floor === "tile" || floor === "brick") {
    const tw = 36, thh = 28;
    for (let y = 0; y < f.h; y += thh) {
      for (let x = 0; x < f.w; x += tw) {
        g.strokeStyle = "rgba(255,255,255,.25)";
        g.strokeRect(f.x + x + (y / thh % 2 ? tw / 2 : 0), f.y + y, tw, thh);
      }
    }
  } else if (floor === "asphalt" || floor === "court") {
    g.fillStyle = floor === "court" ? "#0f172a" : "#78716c";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.strokeStyle = "rgba(253,224,71,.7)";
    g.setLineDash([14, 12]);
    g.lineWidth = 4;
    g.beginPath();
    g.moveTo(f.x + 20, f.y + f.h / 2);
    g.lineTo(f.x + f.w - 20, f.y + f.h / 2);
    g.stroke();
    g.setLineDash([]);
  } else if (floor === "sand") {
    g.fillStyle = "rgba(180,83,9,.18)";
    for (let i = 0; i < 50; i++) g.fillRect(f.x + ((i * 53) % f.w), f.y + ((i * 37) % f.h), 2, 2);
  } else if (floor === "felt") {
    g.fillStyle = "rgba(0,0,0,.12)";
    for (let i = 0; i < 8; i++) g.fillRect(f.x + i * (f.w / 8), f.y, 2, f.h);
  } else if (floor === "stars") {
    g.fillStyle = "rgba(255,255,255,.2)";
    for (let i = 0; i < 24; i++) {
      g.beginPath();
      g.arc(f.x + ((i * 71) % f.w), f.y + ((i * 43) % f.h), 1.5, 0, Math.PI * 2);
      g.fill();
    }
  } else if (floor === "dirt") {
    g.fillStyle = "#a3e635";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.fillStyle = "rgba(120,53,15,.25)";
    for (let i = 0; i < 16; i++) {
      g.beginPath();
      g.ellipse(f.x + 40 + (i % 4) * (f.w / 4), f.y + 50 + ((i / 4) | 0) * (f.h / 4), 28, 12, 0, 0, Math.PI * 2);
      g.fill();
    }
  } else if (floor === "broth") {
    g.fillStyle = "#14532d";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.fillStyle = "rgba(74,222,128,.15)";
    for (let i = 0; i < 10; i++) {
      g.beginPath();
      g.arc(f.x + ((i * 80) % f.w), f.y + ((i * 50) % f.h), 16, 0, Math.PI * 2);
      g.fill();
    }
  } else if (floor === "dusk") {
    const dusk = g.createLinearGradient(0, f.y, 0, f.y + f.h);
    dusk.addColorStop(0, "#fda4af");
    dusk.addColorStop(1, "#fb7185");
    g.fillStyle = dusk;
    g.fillRect(f.x, f.y, f.w, f.h);
    g.fillStyle = "rgba(253,224,71,.18)";
    for (let i = 0; i < 8; i++) {
      g.beginPath();
      g.arc(f.x + 40 + i * (f.w / 8), f.y + f.h * 0.7, 18, 0, Math.PI * 2);
      g.fill();
    }
  } else if (floor === "splats") {
    const blobs = ["#fb7185", "#38bdf8", "#facc15", "#a78bfa", "#4ade80"];
    for (let i = 0; i < 18; i++) {
      g.globalAlpha = 0.28;
      g.fillStyle = blobs[i % blobs.length];
      g.beginPath();
      g.ellipse(f.x + 20 + ((i * 83) % (f.w - 40)), f.y + 20 + ((i * 61) % (f.h - 40)), 18 + i % 10, 12 + i % 8, i, 0, Math.PI * 2);
      g.fill();
    }
    g.globalAlpha = 1;
  } else if (floor === "sparkle") {
    g.fillStyle = "#fde68a";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.fillStyle = "rgba(251,191,36,.45)";
    for (let i = 0; i < 20; i++) {
      const x = f.x + ((i * 71) % f.w), y = f.y + ((i * 47) % f.h);
      g.beginPath();
      g.moveTo(x, y - 6); g.lineTo(x + 3, y); g.lineTo(x, y + 6); g.lineTo(x - 3, y);
      g.fill();
    }
  } else if (floor === "playground") {
    g.fillStyle = "#fdba74";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.strokeStyle = "rgba(255,255,255,.35)";
    g.lineWidth = 4;
    g.strokeRect(f.x + 16, f.y + 16, f.w - 32, f.h - 32);
    g.fillStyle = "rgba(251,113,133,.25)";
    g.beginPath(); g.arc(f.x + f.w * 0.3, f.y + f.h * 0.4, 28, 0, Math.PI * 2); g.fill();
    g.fillStyle = "rgba(56,189,248,.25)";
    g.beginPath(); g.arc(f.x + f.w * 0.7, f.y + f.h * 0.62, 28, 0, Math.PI * 2); g.fill();
  } else if (floor === "soft") {
    g.fillStyle = "#fce7f3";
    g.fillRect(f.x, f.y, f.w, f.h);
    for (let i = 0; i < 8; i++) {
      oval(g, f.x + 40 + i * (f.w / 8), f.y + f.h * 0.5, 22, 14, i % 2 ? "rgba(244,114,182,.2)" : "rgba(196,181,253,.22)");
    }
  } else if (floor === "vault") {
    g.fillStyle = "#ca8a04";
    g.fillRect(f.x, f.y, f.w, f.h);
    g.strokeStyle = "rgba(253,224,71,.45)";
    g.lineWidth = 3;
    for (let i = 0; i < 6; i++) g.strokeRect(f.x + 12 + i * 8, f.y + 12 + i * 8, f.w - 24 - i * 16, f.h - 24 - i * 16);
  } else if (floor === "glass") {
    const gl = g.createLinearGradient(f.x, f.y, f.x + f.w, f.y + f.h);
    gl.addColorStop(0, "rgba(255,255,255,.35)");
    gl.addColorStop(0.5, "rgba(196,181,253,.28)");
    gl.addColorStop(1, "rgba(249,168,212,.22)");
    g.fillStyle = gl;
    g.fillRect(f.x, f.y, f.w, f.h);
    g.strokeStyle = "rgba(255,255,255,.4)";
    for (let i = 0; i < 5; i++) {
      g.beginPath();
      g.moveTo(f.x + i * (f.w / 5), f.y);
      g.lineTo(f.x + i * (f.w / 5) + 40, f.y + f.h);
      g.stroke();
    }
  }
  g.restore();
  g.strokeStyle = "rgba(255,255,255,.62)";
  g.lineWidth = 5;
  g.beginPath();
  g.roundRect(f.x, f.y, f.w, f.h, 28);
  g.stroke();
}

function paintDress(g, ctx, th, layer) {
  const f = ctx.field;
  const t = ctx.t || 0;
  const kind = th.dress;
  if (layer === "far") {
    if (kind === "garden" || kind === "apples" || kind === "yard" || kind === "vines") {
      for (let i = 0; i < 6; i++) tree(g, 30 + i * (ctx.w / 5.5), ctx.h * 0.78, 48 + (i % 3) * 8, i % 2 ? "#16a34a" : "#15803d");
    } else if (kind === "pines") {
      for (let i = 0; i < 7; i++) pine(g, 24 + i * (ctx.w / 6.2), ctx.h * 0.76, 50);
    } else if (kind === "crowd" || kind === "bleachers") {
      for (let r = 0; r < 3; r++) {
        for (let i = 0; i < 16; i++) {
          g.fillStyle = ["#fb7185", "#38bdf8", "#fbbf24", "#a78bfa"][(i + r) % 4];
          g.beginPath();
          g.arc(18 + i * (ctx.w / 16), ctx.h * 0.7 + r * 10, 5, 0, Math.PI * 2);
          g.fill();
        }
      }
    } else if (kind === "skyline") {
      g.fillStyle = "rgba(251,146,60,.55)";
      for (let i = 0; i < 8; i++) g.fillRect(i * (ctx.w / 8), ctx.h * (0.62 - (i % 3) * 0.04), ctx.w / 9, ctx.h * 0.3);
    } else if (kind === "reef") {
      g.fillStyle = "rgba(20,184,166,.35)";
      for (let i = 0; i < 5; i++) {
        g.beginPath();
        g.moveTo(i * (ctx.w / 5), ctx.h);
        g.quadraticCurveTo(i * (ctx.w / 5) + 40, ctx.h * 0.7, i * (ctx.w / 5) + 80, ctx.h);
        g.fill();
      }
    } else if (kind === "moon" || kind === "neon" || kind === "stars") {
      oval(g, ctx.w * 0.18, ctx.h * 0.22, 18, 18, "rgba(248,250,252,.7)");
      oval(g, ctx.w * 0.16, ctx.h * 0.2, 12, 12, th.sky[0]);
    } else if (kind === "kitchen" || kind === "boxes" || kind === "table") {
      g.fillStyle = "rgba(180,83,9,.35)";
      for (let i = 0; i < 5; i++) g.fillRect(20 + i * (ctx.w / 5), ctx.h * 0.68, ctx.w / 7, ctx.h * 0.18);
    } else if (kind === "beach") {
      for (let i = 0; i < 4; i++) {
        g.fillStyle = ["#fb7185", "#38bdf8", "#fbbf24", "#a78bfa"][i];
        g.beginPath();
        g.arc(40 + i * (ctx.w / 4), ctx.h * 0.72, 18, Math.PI, 0);
        g.fill();
        g.fillStyle = "#92400e";
        g.fillRect(38 + i * (ctx.w / 4), ctx.h * 0.72, 4, 28);
      }
    } else if (kind === "lights" || kind === "lanterns") {
      for (let i = 0; i < 7; i++) {
        oval(g, 30 + i * (ctx.w / 6.5), ctx.h * 0.7, 8, 10, i % 2 ? "#fb7185" : "#fde047");
      }
    } else if (kind === "rink" || kind === "cloudbed") {
      for (let i = 0; i < 5; i++) cloud(g, 40 + i * (ctx.w / 5), ctx.h * 0.72, 20, "rgba(255,255,255,.7)");
    }
    return;
  }
  if (kind === "garden" || kind === "apples") {
    const cols = ["#fb7185", "#fbbf24", "#f472b6", "#fde047"];
    for (let i = 0; i < 14; i++) {
      const x = f.x + 16 + (i * 79) % (f.w - 32);
      const y = f.y + f.h - 18;
      oval(g, x, y, 5, 5, cols[i % cols.length]);
      g.fillStyle = "#16a34a";
      g.fillRect(x - 1, y + 3, 2, 6);
    }
  }
  if (kind === "sweets") {
    for (let i = 0; i < 5; i++) {
      const x = f.x + 30 + i * (f.w / 5);
      oval(g, x, f.y + 36, 10, 10, i % 2 ? "#fb7185" : "#38bdf8");
      g.fillStyle = "#fde68a";
      g.fillRect(x - 2, f.y + 44, 4, 16);
    }
  }
  if (kind === "balloons") {
    for (let i = 0; i < 6; i++) {
      const x = f.x + 24 + i * (f.w / 6);
      const y = f.y + 28 + Math.sin(t * 2 + i) * 6;
      oval(g, x, y, 9, 12, ["#fb7185", "#38bdf8", "#fbbf24"][i % 3]);
      g.strokeStyle = "rgba(12,74,110,.35)";
      g.beginPath();
      g.moveTo(x, y + 12);
      g.lineTo(x, y + 28);
      g.stroke();
    }
  }
  if (kind === "bubbles" || kind === "reef") {
    g.fillStyle = "rgba(255,255,255,.28)";
    for (let i = 0; i < 10; i++) {
      g.beginPath();
      g.arc(f.x + ((i * 70 + t * 20) % f.w), f.y + f.h - ((t * 30 + i * 40) % f.h), 5 + i % 4, 0, Math.PI * 2);
      g.fill();
    }
  }
  if (kind === "steam") {
    g.fillStyle = "rgba(255,255,255,.18)";
    for (let i = 0; i < 6; i++) oval(g, f.x + 40 + i * 70, f.y + 30 + Math.sin(t * 2 + i) * 8, 16, 8, "rgba(255,255,255,.2)");
  }
  if (kind === "neon") {
    g.strokeStyle = "rgba(34,211,238,.35)";
    g.lineWidth = 3;
    g.strokeRect(f.x + 10, f.y + 10, f.w - 20, f.h - 20);
  }
  if (kind === "mirrors" || kind === "glass") {
    for (let i = 0; i < 4; i++) {
      g.fillStyle = "rgba(255,255,255,.22)";
      g.beginPath();
      g.roundRect(f.x + 18 + i * (f.w / 4), f.y + 16, 28, f.h * 0.28, 8);
      g.fill();
    }
  }
  if (kind === "toys") {
    const cols = ["#fb7185", "#38bdf8", "#fbbf24", "#4ade80"];
    for (let i = 0; i < 8; i++) {
      g.fillStyle = cols[i % 4];
      g.beginPath();
      g.roundRect(f.x + 20 + i * (f.w / 8), f.y + f.h - 28, 16, 16, 4);
      g.fill();
    }
  }
  if (kind === "table") {
    g.fillStyle = "rgba(91,33,182,.35)";
    g.fillRect(f.x + 20, f.y + f.h - 22, f.w - 40, 10);
  }
  if (kind === "cloudbed") {
    const fluff = ["#fb7185", "#a78bfa", "#38bdf8", "#fde047", "#f9a8d4", "#c4b5fd"];
    for (let i = 0; i < 6; i++) cloud(g, f.x + 30 + i * (f.w / 6), f.y + f.h - 18, 16, fluff[i]);
  }
  if (kind === "lanterns") {
    for (let i = 0; i < 5; i++) {
      const x = f.x + 30 + i * (f.w / 5);
      oval(g, x, f.y + 22, 8, 10, i % 2 ? "#fb923c" : "#fde047");
      g.strokeStyle = "rgba(12,74,110,.3)";
      g.beginPath(); g.moveTo(x, f.y + 8); g.lineTo(x, f.y + 14); g.stroke();
    }
  }
  if (kind === "coins") {
    for (let i = 0; i < 10; i++) {
      oval(g, f.x + 24 + (i * 67) % (f.w - 40), f.y + f.h - 22, 7, 7, "#facc15");
    }
  }
  if (kind === "kitchen") {
    g.fillStyle = "rgba(251,146,60,.35)";
    g.fillRect(f.x + 12, f.y + 12, 48, 18);
    g.fillRect(f.x + f.w - 70, f.y + 12, 52, 18);
    g.fillStyle = "rgba(255,255,255,.4)";
    g.fillRect(f.x + 18, f.y + 16, 12, 8);
  }
  if (kind === "boxes") {
    for (let i = 0; i < 4; i++) {
      g.fillStyle = "#fbbf24";
      g.beginPath();
      g.roundRect(f.x + 16 + i * 36, f.y + f.h - 34, 22, 18, 4);
      g.fill();
    }
  }
  if (kind === "lights") {
    g.fillStyle = "rgba(253,224,71,.25)";
    for (let i = 0; i < 6; i++) {
      g.beginPath();
      g.arc(f.x + 30 + i * (f.w / 6), f.y + 18, 8, 0, Math.PI * 2);
      g.fill();
    }
  }
  if (kind === "rink") {
    g.strokeStyle = "rgba(255,255,255,.5)";
    g.lineWidth = 3;
    g.beginPath();
    g.ellipse(f.x + f.w / 2, f.y + f.h / 2, f.w * 0.38, f.h * 0.32, 0, 0, Math.PI * 2);
    g.stroke();
  }
  if (kind === "birds") {
    g.strokeStyle = "rgba(12,74,110,.35)";
    g.lineWidth = 2;
    for (let i = 0; i < 5; i++) {
      const x = f.x + 40 + i * 70, y = f.y + 24 + Math.sin(t + i) * 4;
      g.beginPath(); g.moveTo(x - 8, y); g.quadraticCurveTo(x, y - 6, x + 8, y); g.stroke();
    }
  }
  if (kind === "drips") {
    const cols = ["#fb7185", "#38bdf8", "#a78bfa", "#facc15"];
    for (let i = 0; i < 10; i++) {
      g.fillStyle = cols[i % 4];
      g.globalAlpha = 0.55;
      g.beginPath();
      g.ellipse(f.x + 20 + i * (f.w / 10), f.y + 12, 10, 16, 0, 0, Math.PI * 2);
      g.fill();
    }
    g.globalAlpha = 1;
  }
  if (kind === "crowd" || kind === "bleachers") {
    g.fillStyle = "rgba(255,255,255,.2)";
    g.fillRect(f.x + 8, f.y + 8, f.w - 16, 10);
  }
  if (kind === "beach") {
    g.fillStyle = "#fb923c";
    g.fillRect(f.x + 20, f.y + f.h - 14, 40, 6);
    g.fillStyle = "#38bdf8";
    g.beginPath(); g.arc(f.x + 40, f.y + f.h - 14, 16, Math.PI, 0); g.fill();
  }
  if (kind === "yard") {
    for (let i = 0; i < 4; i++) {
      g.fillStyle = "#f9a8d4";
      g.beginPath(); g.arc(f.x + 30 + i * 50, f.y + f.h - 16, 6, 0, Math.PI * 2); g.fill();
    }
  }
  if (kind === "stars" || kind === "moon") {
    g.fillStyle = "rgba(253,224,71,.45)";
    for (let i = 0; i < 8; i++) {
      g.beginPath();
      g.arc(f.x + ((i * 90) % f.w), f.y + 16 + (i % 3) * 10, 2, 0, Math.PI * 2);
      g.fill();
    }
  }
}

export function paintBuddy(g, p, t, emoji) {
  const bob = p._ground && Math.abs(p.vx) > 20 ? Math.sin((t || 0) * 14) * 2.2 : 0;
  g.save();
  g.translate(p.x, p.y + bob);
  g.scale(1, p.squish || 1);
  g.fillStyle = "rgba(12,74,110,.2)";
  g.beginPath();
  g.ellipse(0, p.r * 0.98, p.r * 0.9, p.r * 0.28, 0, 0, Math.PI * 2);
  g.fill();
  g.fillStyle = p.id === "alon" ? "#fda4af" : "#7dd3fc";
  g.beginPath();
  g.ellipse(-p.r * 0.45, p.r * 0.72, p.r * 0.28, p.r * 0.16, 0, 0, Math.PI * 2);
  g.fill();
  g.beginPath();
  g.ellipse(p.r * 0.45, p.r * 0.72, p.r * 0.28, p.r * 0.16, 0, 0, Math.PI * 2);
  g.fill();
  g.beginPath();
  g.fillStyle = p.color;
  g.arc(0, 0, p.r, 0, Math.PI * 2);
  g.fill();
  g.strokeStyle = "rgba(255,255,255,.6)";
  g.lineWidth = 3;
  g.stroke();
  g.fillStyle = "rgba(255,255,255,.38)";
  g.beginPath();
  g.arc(-p.r * 0.28, -p.r * 0.3, p.r * 0.34, 0, Math.PI * 2);
  g.fill();
  g.fillStyle = p.id === "alon" ? "#fecdd3" : "#bae6fd";
  g.beginPath();
  g.arc(-p.r * 0.42, p.r * 0.08, p.r * 0.16, 0, Math.PI * 2);
  g.arc(p.r * 0.42, p.r * 0.08, p.r * 0.16, 0, Math.PI * 2);
  g.fill();
  g.fillStyle = "#fff";
  g.beginPath();
  g.arc(-p.r * 0.28, -p.r * 0.1, p.r * 0.2, 0, Math.PI * 2);
  g.arc(p.r * 0.28, -p.r * 0.1, p.r * 0.2, 0, Math.PI * 2);
  g.fill();
  g.fillStyle = "#1e293b";
  const glance = (p.facing || 1) * p.r * 0.08;
  g.beginPath();
  g.arc(-p.r * 0.22 + glance, -p.r * 0.08, p.r * 0.09, 0, Math.PI * 2);
  g.arc(p.r * 0.34 + glance, -p.r * 0.08, p.r * 0.09, 0, Math.PI * 2);
  g.fill();
  g.strokeStyle = "#1e293b";
  g.lineWidth = 2.6;
  g.beginPath();
  g.arc(0, p.r * 0.18, p.r * 0.34, 0.2, Math.PI - 0.2);
  g.stroke();
  if (p.id === "alon") {
    g.fillStyle = "#fde047";
    g.beginPath();
    g.arc(0, -p.r * 0.92, p.r * 0.22, 0, Math.PI * 2);
    g.fill();
    g.beginPath();
    g.arc(-p.r * 0.2, -p.r * 1.02, p.r * 0.14, 0, Math.PI * 2);
    g.arc(p.r * 0.2, -p.r * 1.02, p.r * 0.14, 0, Math.PI * 2);
    g.fill();
  } else {
    g.fillStyle = "#0c4a6e";
    g.beginPath();
    g.roundRect(-p.r * 0.7, -p.r * 1.05, p.r * 1.4, p.r * 0.32, 8);
    g.fill();
    g.fillStyle = "#38bdf8";
    g.fillRect(-p.r * 0.15, -p.r * 1.05, p.r * 0.3, p.r * 0.32);
  }
  if (emoji) {
    g.font = `${p.r * 0.85}px serif`;
    g.textAlign = "center";
    g.textBaseline = "middle";
    g.fillText(emoji, p.r * 0.62, -p.r * 0.55);
  }
  g.restore();
}

export function paintPlat(g, p, color) {
  const top = p.bounce ? "#f472b6" : p.ice ? "#e0f2fe" : p.hurt ? "#fb923c" : (color || "#4ade80");
  g.fillStyle = p.ice ? "#7dd3fc" : p.bounce ? "#be185d" : "#15803d";
  g.beginPath();
  g.roundRect(p.x, p.y + 6, p.w, p.h, 10);
  g.fill();
  g.fillStyle = top;
  g.beginPath();
  g.roundRect(p.x, p.y, p.w, Math.max(12, p.h - 4), 10);
  g.fill();
  g.fillStyle = "rgba(255,255,255,.4)";
  g.fillRect(p.x + 8, p.y + 4, p.w - 16, 4);
  if (p.bounce) {
    g.fillStyle = "#fb7185";
    g.beginPath();
    g.arc(p.x + p.w / 2, p.y + p.h / 2, 8, 0, Math.PI * 2);
    g.fill();
    g.fillStyle = "#fff";
    g.beginPath();
    g.arc(p.x + p.w / 2 - 3, p.y + p.h / 2 - 2, 2.5, 0, Math.PI * 2);
    g.fill();
  }
  if (p.ice) {
    g.strokeStyle = "rgba(255,255,255,.7)";
    g.beginPath();
    g.moveTo(p.x + 10, p.y + 8);
    g.lineTo(p.x + p.w * 0.4, p.y + 8);
    g.stroke();
  }
}

export function paintIcon(g, x, y, emoji, color, r, t) {
  const rad = r || 14;
  const hop = Math.sin((t || 0) * 6 + x * 0.05) * 2;
  g.save();
  g.translate(x, y + hop);
  g.fillStyle = color || "#fbbf24";
  g.beginPath();
  g.arc(0, 0, rad, 0, Math.PI * 2);
  g.fill();
  g.fillStyle = "rgba(255,255,255,.35)";
  g.beginPath();
  g.arc(-rad * 0.28, -rad * 0.28, rad * 0.35, 0, Math.PI * 2);
  g.fill();
  g.strokeStyle = "rgba(255,255,255,.75)";
  g.lineWidth = 2;
  g.beginPath();
  g.arc(0, 0, rad, 0, Math.PI * 2);
  g.stroke();
  g.font = `${rad * 1.55}px serif`;
  g.textAlign = "center";
  g.textBaseline = "middle";
  g.fillText(emoji, 0, 1);
  g.restore();
}

export function paintLabel(g, p, text) {
  g.save();
  g.font = "800 16px Trebuchet MS, sans-serif";
  g.textAlign = "center";
  g.lineWidth = 4;
  g.strokeStyle = "rgba(12,74,110,.4)";
  g.strokeText(text || p.name, p.x, p.y - p.r - 14);
  g.fillStyle = p.color;
  g.fillText(text || p.name, p.x, p.y - p.r - 14);
  g.restore();
}

function shine(g, x, y, r) {
  g.fillStyle = "rgba(255,255,255,.42)";
  g.beginPath();
  g.arc(x - r * 0.28, y - r * 0.28, r * 0.32, 0, Math.PI * 2);
  g.fill();
}

export function paintProp(g, kind, x, y, r, t, extra) {
  const rad = r || 14;
  const hop = Math.sin((t || 0) * 6 + x * 0.04) * 2;
  g.save();
  g.translate(x, y + hop);
  const k = kind || "coin";
  if (k === "coin" || k === "star") {
    g.fillStyle = k === "star" ? "#fde047" : "#fbbf24";
    g.beginPath();
    if (k === "star") {
      for (let i = 0; i < 5; i++) {
        const a = -Math.PI / 2 + i * Math.PI * 0.8;
        const rr = i % 2 ? rad * 0.45 : rad;
        const fn = i ? g.lineTo.bind(g) : g.moveTo.bind(g);
        fn(Math.cos(a) * rr, Math.sin(a) * rr);
      }
      g.closePath();
    } else g.arc(0, 0, rad, 0, Math.PI * 2);
    g.fill();
    g.strokeStyle = "rgba(180,83,9,.45)";
    g.lineWidth = 2;
    g.stroke();
    shine(g, 0, 0, rad);
    if (k === "coin") {
      g.fillStyle = "rgba(180,83,9,.45)";
      g.font = `800 ${rad}px Trebuchet MS, sans-serif`;
      g.textAlign = "center";
      g.textBaseline = "middle";
      g.fillText("$", 0, 1);
    }
  } else if (k === "pearl") {
    g.fillStyle = "#f8fafc";
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.fill();
    g.strokeStyle = "rgba(125,211,252,.8)"; g.lineWidth = 2; g.stroke();
    shine(g, 0, 0, rad);
  } else if (k === "jelly") {
    g.fillStyle = extra && extra.color || "#c4b5fd";
    g.beginPath();
    g.ellipse(0, 0, rad, rad * 0.85, 0, Math.PI, 0);
    g.lineTo(rad, rad * 0.7);
    for (let i = 4; i >= 0; i--) g.quadraticCurveTo(-rad + i * rad * 0.5, rad * 1.1, -rad + (i - 0.5) * rad * 0.5, rad * 0.55);
    g.closePath();
    g.fill();
    shine(g, 0, -2, rad);
  } else if (k === "mush") {
    g.fillStyle = "#fef3c7";
    g.beginPath(); g.roundRect(-rad * 0.28, 0, rad * 0.56, rad * 0.7, 4); g.fill();
    g.fillStyle = "#fb7185";
    g.beginPath(); g.ellipse(0, 0, rad, rad * 0.62, 0, Math.PI, 0, true); g.fill();
    g.fillStyle = "#fff";
    g.beginPath(); g.arc(-rad * 0.3, -rad * 0.1, rad * 0.16, 0, Math.PI * 2); g.fill();
    g.beginPath(); g.arc(rad * 0.25, -rad * 0.18, rad * 0.12, 0, Math.PI * 2); g.fill();
  } else if (k === "crate") {
    g.fillStyle = "#f59e0b";
    g.beginPath(); g.roundRect(-rad, -rad, rad * 2, rad * 2, 6); g.fill();
    g.strokeStyle = "rgba(146,64,14,.55)"; g.lineWidth = 2;
    g.strokeRect(-rad + 3, -rad + 3, rad * 2 - 6, rad * 2 - 6);
    g.beginPath(); g.moveTo(-rad + 4, -rad + 4); g.lineTo(rad - 4, rad - 4);
    g.moveTo(rad - 4, -rad + 4); g.lineTo(-rad + 4, rad - 4); g.stroke();
  } else if (k === "pillow") {
    g.fillStyle = extra && extra.color || "#f472b6";
    g.beginPath(); g.roundRect(-rad * 1.25, -rad * 0.75, rad * 2.5, rad * 1.5, 14); g.fill();
    g.strokeStyle = "rgba(255,255,255,.7)";
    g.lineWidth = 3;
    g.stroke();
    g.fillStyle = "rgba(255,255,255,.45)";
    g.fillRect(-rad * 0.6, -rad * 0.35, rad, 6);
  } else if (k === "snowman") {
    oval(g, 0, rad * 0.35, rad * 0.85, rad * 0.7, "#fff");
    oval(g, 0, -rad * 0.45, rad * 0.58, rad * 0.5, "#fff");
    g.fillStyle = "#1e293b";
    g.beginPath(); g.arc(-rad * 0.16, -rad * 0.5, 2.2, 0, Math.PI * 2); g.arc(rad * 0.16, -rad * 0.5, 2.2, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#fb923c";
    g.beginPath(); g.moveTo(0, -rad * 0.4); g.lineTo(rad * 0.35, -rad * 0.32); g.lineTo(0, -rad * 0.28); g.fill();
  } else if (k === "ghost") {
    g.fillStyle = "rgba(226,232,240,.92)";
    g.beginPath();
    g.arc(0, -rad * 0.15, rad, Math.PI, 0);
    g.lineTo(rad, rad);
    for (let i = 3; i >= 0; i--) g.quadraticCurveTo(-rad + i * rad * 0.5, rad * 1.25, -rad + (i - 0.5) * rad * 0.5, rad * 0.7);
    g.closePath();
    g.fill();
    g.fillStyle = "#1e293b";
    g.beginPath(); g.arc(-rad * 0.28, -rad * 0.15, rad * 0.16, 0, Math.PI * 2); g.arc(rad * 0.28, -rad * 0.15, rad * 0.16, 0, Math.PI * 2); g.fill();
  } else if (k === "pet") {
    const col = extra && extra.color || "#f9a8d4";
    oval(g, 0, 2, rad * 0.95, rad * 0.8, col);
    oval(g, -rad * 0.55, -rad * 0.55, rad * 0.28, rad * 0.38, col);
    oval(g, rad * 0.55, -rad * 0.55, rad * 0.28, rad * 0.38, col);
    g.fillStyle = "#fff";
    g.beginPath(); g.arc(-rad * 0.22, -2, rad * 0.18, 0, Math.PI * 2); g.arc(rad * 0.22, -2, rad * 0.18, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#1e293b";
    g.beginPath(); g.arc(-rad * 0.18, -1, 2.2, 0, Math.PI * 2); g.arc(rad * 0.26, -1, 2.2, 0, Math.PI * 2); g.fill();
  } else if (k === "balloon") {
    const col = extra && extra.color || "#fb7185";
    oval(g, 0, 0, rad * 0.85, rad, col);
    g.fillStyle = col;
    g.beginPath(); g.moveTo(-4, rad * 0.9); g.lineTo(4, rad * 0.9); g.lineTo(0, rad * 1.15); g.fill();
    g.strokeStyle = "rgba(12,74,110,.35)";
    g.beginPath(); g.moveTo(0, rad * 1.15); g.lineTo(0, rad * 1.8); g.stroke();
    shine(g, 0, 0, rad);
  } else if (k === "potato") {
    oval(g, 0, 0, rad * 1.1, rad * 0.8, "#fdba74");
    g.fillStyle = "rgba(146,64,14,.35)";
    g.beginPath(); g.arc(-rad * 0.3, -rad * 0.1, 2, 0, Math.PI * 2); g.arc(rad * 0.2, rad * 0.15, 2, 0, Math.PI * 2); g.fill();
    shine(g, 0, 0, rad);
  } else if (k === "soccer" || k === "volley" || k === "puck" || k === "ball") {
    g.fillStyle = k === "puck" ? "#0c4a6e" : "#f8fafc";
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.fill();
    g.strokeStyle = k === "volley" ? "#f97316" : "#1e293b";
    g.lineWidth = 2;
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.stroke();
    if (k === "soccer") {
      g.beginPath(); g.arc(0, 0, rad * 0.35, 0, Math.PI * 2); g.fillStyle = "#1e293b"; g.fill();
    }
  } else if (k === "cocoa") {
    g.fillStyle = "#b45309";
    g.beginPath(); g.roundRect(-rad * 0.55, -rad * 0.2, rad * 1.1, rad * 1.1, 4); g.fill();
    g.fillStyle = "#7c2d12";
    g.fillRect(-rad * 0.45, -rad * 0.05, rad * 0.9, 8);
    g.strokeStyle = "#fde68a"; g.beginPath(); g.arc(rad * 0.7, 4, rad * 0.28, -0.8, 0.8); g.stroke();
  } else if (k === "flake") {
    g.strokeStyle = "#e0f2fe"; g.lineWidth = 2;
    for (let i = 0; i < 3; i++) {
      g.beginPath(); g.moveTo(0, -rad); g.lineTo(0, rad); g.stroke();
      g.rotate(Math.PI / 3);
    }
  } else if (k === "fruit") {
    const col = extra && extra.color || "#fb7185";
    oval(g, 0, 2, rad * 0.9, rad, col);
    g.fillStyle = "#16a34a";
    g.fillRect(-2, -rad, 4, 8);
    shine(g, 0, 2, rad);
  } else if (k === "veg") {
    g.fillStyle = "#4ade80";
    g.beginPath();
    g.moveTo(0, -rad); g.lineTo(rad * 0.7, rad * 0.6); g.lineTo(-rad * 0.7, rad * 0.6);
    g.closePath(); g.fill();
    g.fillStyle = "#166534"; g.fillRect(-2, rad * 0.5, 4, 8);
  } else if (k === "mole") {
    const col = extra && extra.color || "#fb7185";
    oval(g, 0, 4, rad * 0.95, rad * 0.75, col);
    oval(g, -rad * 0.55, -rad * 0.2, rad * 0.28, rad * 0.32, col);
    oval(g, rad * 0.55, -rad * 0.2, rad * 0.28, rad * 0.32, col);
    g.fillStyle = "#fff";
    g.beginPath(); g.arc(-rad * 0.22, 0, rad * 0.18, 0, Math.PI * 2); g.arc(rad * 0.22, 0, rad * 0.18, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#1e293b";
    g.beginPath(); g.arc(-rad * 0.18, 1, 2.2, 0, Math.PI * 2); g.arc(rad * 0.26, 1, 2.2, 0, Math.PI * 2); g.fill();
  } else if (k === "bite") {
    g.fillStyle = "#fbbf24";
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#b45309";
    g.beginPath(); g.arc(0, 4, rad * 0.55, 0.2, Math.PI - 0.2); g.fill();
    shine(g, 0, 0, rad);
  } else if (k === "rock") {
    g.fillStyle = extra && extra.gold ? "#fde047" : "#94a3b8";
    g.beginPath();
    g.moveTo(-rad, 4); g.lineTo(-rad * 0.4, -rad); g.lineTo(rad * 0.5, -rad * 0.6); g.lineTo(rad, 6); g.closePath();
    g.fill();
    shine(g, 0, 0, rad);
  } else if (k === "blob") {
    g.fillStyle = extra && extra.color || "#c4b5fd";
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#fff";
    g.beginPath(); g.arc(-rad * 0.28, -rad * 0.12, rad * 0.22, 0, Math.PI * 2); g.arc(rad * 0.3, -rad * 0.12, rad * 0.22, 0, Math.PI * 2); g.fill();
    g.fillStyle = "#1e293b";
    g.beginPath(); g.arc(-rad * 0.22, -rad * 0.08, rad * 0.1, 0, Math.PI * 2); g.arc(rad * 0.36, -rad * 0.08, rad * 0.1, 0, Math.PI * 2); g.fill();
  } else if (k === "key") {
    g.fillStyle = "#fde047";
    g.beginPath(); g.arc(-rad * 0.3, 0, rad * 0.45, 0, Math.PI * 2); g.fill();
    g.fillRect(0, -3, rad, 6);
    g.fillRect(rad * 0.4, 2, 4, 8);
    g.fillStyle = "#b45309";
    g.beginPath(); g.arc(-rad * 0.3, 0, rad * 0.18, 0, Math.PI * 2); g.fill();
  } else if (k === "apple") {
    oval(g, 0, 2, rad * 0.85, rad, "#fb7185");
    g.fillStyle = "#16a34a"; g.fillRect(-1, -rad, 3, 8);
  } else {
    g.fillStyle = extra && extra.color || "#fbbf24";
    g.beginPath(); g.arc(0, 0, rad, 0, Math.PI * 2); g.fill();
    shine(g, 0, 0, rad);
  }
  g.restore();
}

export function paintHead(g, x, y, who, r) {
  const p = {
    x, y, r: r || 16,
    id: who === "dad" ? "dad" : "alon",
    name: who === "dad" ? "Dad" : "Alon",
    color: who === "dad" ? "#38bdf8" : "#fb7185",
    facing: who === "dad" ? -1 : 1,
    squish: 1
  };
  paintBuddy(g, p, 0);
  paintLabel(g, p);
}
